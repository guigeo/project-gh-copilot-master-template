name = "python-minimal"
description = "Python com contexto mínimo no bootstrap (sem agents, skills e prompts)."
extends = ["common"]
packs = ["python"]
exclude_categories = ["agents", "skills", "prompts"]
