name = "pyspark"
description = "PySpark: instrução de boas práticas Spark, skill de otimização de jobs, agente revisor, prompt de otimização e scaffold com SparkSession testável. Requer o pack python."

# Dependências extras compostas no pyproject do projeto gerado (engine: ver _template_lib).
[python]
dependencies = ["pyspark>=3.5.1"]
